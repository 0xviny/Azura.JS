const { Server } = require('@atosjs/azura');
const HelloController = require('./controllers/HelloController.js').HelloController;

const app = new Server({ port: 3000, cluster: false });

app.load([HelloController]);
app.listen();