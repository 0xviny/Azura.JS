const { Controller, Get } = require("@atosjs/azura");

class HelloController {
  async index(ctx) {
    ctx.response.writeHead(200, { "Content-Type": "application/json" });
    ctx.response.end(JSON.stringify({ message: "Olá do Test!" }));
  }
}

Controller("/hello")(HelloController);
Get("/")(HelloController.prototype, "index");

module.exports = { HelloController };
